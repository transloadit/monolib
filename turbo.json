{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env.*local"],
  "tasks": {
    "lint": {
      "outputs": []
    },
    "test": {
      "inputs": ["src/**/*.ts", "src/**/*.tsx"],
      "outputs": []
    },
    "test:watch": {
      "cache": false,
      "persistent": true,
      "outputs": []
    }
  }
}
