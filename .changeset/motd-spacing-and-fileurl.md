---
'@transloadit/sev-logger': patch
---

- Normalize file:// callsite URLs to filesystem paths so callsite links stay relative and readable.
- Ensure MOTD output has a space after ISO timestamps for cleaner startup logs.
