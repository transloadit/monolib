{
  "name": "@transloadit/replace-me",
  "version": "0.0.0",
  "private": false,
  "repository": {
    "type": "git",
    "url": "git://github.com/transloadit/monolib.git",
    "directory": "packages/replace-me"
  },
  "license": "AGPL-3.0-only",
  "main": "dist/replaceMe.js",
  "typings": "dist/replaceMe.d.ts",
  "directories": {
    "lib": "dist",
    "test": "dist"
  },
  "scripts": {
    "test": "echo \"Error: run tests from root\" && exit 1",
    "tsc": "tsc --build --clean && tsc --build"
  },
  "publishConfig": {
    "access": "public"
  }
}
