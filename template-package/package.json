{
  "name": "@transloadit/replace-me",
  "version": "0.0.1",
  "private": false,
  "repository": {
    "type": "git",
    "url": "git://github.com/transloadit/monolib.git",
    "directory": "packages/replace-me"
  },
  "license": "MIT",
  "main": "dist/replaceMe.js",
  "types": "dist/replaceMe.d.ts",
  "files": [
    "dist",
    "src",
    "!*.test.*"
  ],
  "scripts": {
    "prepack": "tsc --build",
    "test": "tsc --build && node --test --enable-source-maps",
    "test:watch": "node --test --enable-source-maps --watch"
  },
  "publishConfig": {
    "access": "public"
  }
}
